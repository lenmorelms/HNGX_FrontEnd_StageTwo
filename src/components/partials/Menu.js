import React from "react";

const Menu = () => {
    return (
        <div className="header-menu flex-row">
            <span>Sign in</span>
            <span>Menu Icon</span>
        </div>
    );
};

export default Menu;